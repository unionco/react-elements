import React from 'react';

const {
  Consumer,
  Provider,
} = React.createContext();

export const RangeConsumer = Consumer;
export const RangeProvider = Provider;

export default {
  Consumer,
  Provider,
}
